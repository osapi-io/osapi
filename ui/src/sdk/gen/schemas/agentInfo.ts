/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Agent Management API
 * OpenAPI spec version: 1.0.0
 */
import type { AgentInfoFacts } from './agentInfoFacts';
import type { AgentInfoLabels } from './agentInfoLabels';
import type { AgentInfoState } from './agentInfoState';
import type { AgentInfoStatus } from './agentInfoStatus';
import type { LoadAverageResponse } from './loadAverageResponse';
import type { MemoryResponse } from './memoryResponse';
import type { NetworkInterfaceResponse } from './networkInterfaceResponse';
import type { NodeCondition } from './nodeCondition';
import type { OSInfoResponse } from './oSInfoResponse';
import type { RouteResponse } from './routeResponse';
import type { TimelineEvent } from './timelineEvent';

export interface AgentInfo {
  /** The hostname of the agent. */
  hostname: string;
  /** The current status of the agent. */
  status: AgentInfoStatus;
  /** Key-value labels configured on the agent. */
  labels?: AgentInfoLabels;
  /** When the agent last refreshed its heartbeat. */
  registered_at?: string;
  /** When the agent process started. */
  started_at?: string;
  os_info?: OSInfoResponse;
  /** The system uptime. */
  uptime?: string;
  load_average?: LoadAverageResponse;
  memory?: MemoryResponse;
  /** CPU architecture. */
  architecture?: string;
  /** OS kernel version. */
  kernel_version?: string;
  /** Number of logical CPUs. */
  cpu_count?: number;
  /** Fully qualified domain name. */
  fqdn?: string;
  /** Init system. */
  service_mgr?: string;
  /** Package manager. */
  package_mgr?: string;
  interfaces?: NetworkInterfaceResponse[];
  /** Name of the interface used for the default route. */
  primary_interface?: string;
  /** Network routing table entries. */
  routes?: RouteResponse[];
  /** Extended facts from additional providers. */
  facts?: AgentInfoFacts;
  /** Agent scheduling state. */
  state?: AgentInfoState;
  /** Evaluated node conditions. */
  conditions?: NodeCondition[];
  /** Agent state transition history. */
  timeline?: TimelineEvent[];
}
