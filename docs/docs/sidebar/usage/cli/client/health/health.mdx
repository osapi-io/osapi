# Health

CLI to check the health of the API server.

Running `osapi client health` without a subcommand shows available subcommands.
Use `osapi client health liveness` to perform a liveness probe.

import DocCardList from '@theme/DocCardList';

<DocCardList />
