# Node

CLI for node operations — system queries, network management, and command
execution. These commands dispatch jobs to agents targeting the specified host.

For fleet discovery from the agent registry, see [Agent](../agent/agent.mdx)
commands.

import DocCardList from '@theme/DocCardList';

<DocCardList />
