# Node

CLI to manage node resources. Includes fleet discovery from the agent registry
(`list`, `get`) and job-based system queries (`status`, `hostname`).

import DocCardList from '@theme/DocCardList';

<DocCardList />
