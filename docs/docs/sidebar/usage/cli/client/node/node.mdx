# Node

CLI for job-based node system queries (`status`, `hostname`). These commands
dispatch jobs to agents and return system-level information.

For fleet discovery from the agent registry, see [Agent](../agent/agent.mdx)
commands.

import DocCardList from '@theme/DocCardList';

<DocCardList />
