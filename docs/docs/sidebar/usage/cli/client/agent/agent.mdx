# Agent

CLI to manage agents in the fleet. Agent discovery, lifecycle, and PKI
enrollment commands. Most commands read directly from the NATS KV heartbeat
registry -- no jobs are created.

import DocCardList from '@theme/DocCardList';

<DocCardList />
